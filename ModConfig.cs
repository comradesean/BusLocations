namespace BusLocations
{
    /// <summary>The mod configuration.</summary>
    public class ModConfig
    {
        /// <summary>Whether Pam must be at the bus stop for the bus to operate.</summary>
        public bool RequirePam { get; set; } = false;
    }
}
